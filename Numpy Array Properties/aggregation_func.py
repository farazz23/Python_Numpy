import numpy as np


arr = np.array([10, 20, 30, 40, 50, 60])

# TODO : All the aggregation function which is used in a real world project
print(np.sum(arr))
print(np.min(arr))
print(np.max(arr))
print(np.mean(arr))
print(np.std(arr))
print(np.var(arr))
print(np.average(arr))
